function add(a, b) {
    return a +b;
}

function subtract(a, b) {
    return a - b;
    // a has to be the bigger number
    // minus * minus = plus...
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
    // round numbers
    // case for dividing by 0
}

